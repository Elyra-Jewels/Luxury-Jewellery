<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Glassmorphic Dashboard</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: url('https://wallpapercave.com/wp/wp5128415.jpg') no-repeat center center/cover;
      color: white;
      overflow: hidden;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      backdrop-filter: blur(4px);
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 30px;
      letter-spacing: 4px;
      background: rgba(0, 0, 0, 0.6);
      padding: 10px 20px;
      border-radius: 12px;
    }

    .grid {
      display: flex;
      gap: 30px;
    }

    .card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 25px;
      padding: 40px;
      text-align: center;
      backdrop-filter: blur(10px);
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
      width: 120px;
      height: 120px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(255, 255, 255, 0.2);
      transition: 0.3s;
    }

    .card:hover {
      transform: scale(1.05);
      box-shadow: 0 0 30px rgba(255, 255, 255, 0.3);
    }

    .card img {
      width: 40px;
      height: 40px;
      filter: invert(1);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>CONTENT</h1>
    <div class="grid">
      <div class="card">
        <img src="icon-headset.png" alt="Intro" />
      </div>
      <div class="card">
        <img src="icon-network.png" alt="Network" />
      </div>
      <div class="card">
        <img src="icon-shield.png" alt="Security" />
      </div>
      <div class="card">
        <img src="icon-chess.png" alt="Strategy" />
      </div>
    </div>
  </div>
</body>
</html>
